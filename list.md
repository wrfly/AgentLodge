# 待选清单

> 从 [RESEARCH.md](./RESEARCH.md) 整理出来的候选项，一条一个勾选框。
> 在 `[ ]` 里打个 `x`，或者直接在行首画 `x` 都行 —— 你选完我按编号做。
>
> 工期是**粗估**，按一个人、含测试和文档；标「风险」的是改核心路径或会动数据。

---

## 先定方向（不是任务，但影响 E 段怎么排）

这两条路不互斥，但同时走等于两边各做一半。选一条，或者明确说「先都不选，只做 A/B/C 段」。

- [ ] **方向 A — 团队拼车网关**：多凭据池调度、分组倍率、支付/兑换码、OIDC、webhook。
      主要投入在网关和后台，前端几乎不动。对手是 sub2api、new-api。
- [ ] **方向 B — 团队版 Claude Code on the web**：持久项目 + git 面板、审批 UI + plan 模式、
      MCP、定时任务、手机推送。主要投入在前端和容器编排，网关基本不动。
      对手是 CloudCLI、Anthropic 官方 web。

---

## A · 紧急修复（线上正在出问题）

- [ ] **A1 修 DeepSeek 价格表** — 9/10 新 ID `deepseek-flash` 表里没有，落到 `*` 兜底的
      $5/$25，按金额计费贵 33 倍；`deepseek-v4-pro` 种子价是 4 月 preview 价，少算一半。
      改 `pricing.ts:256` + `model-facts.ts:51`，**线上库要在后台手动改一遍**。（粗估：半天）
- [ ] **A2 价格表支持分时** — DeepSeek 周一至五 UTC 01–04、06–10 高峰价 ×2，现在一行只能填一个数。
      两种做法二选一：
      - [ ] A2-min 只在后台显示「当前是高峰/非高峰」+ 补 note，不动计费（粗估：1 小时）
      - [ ] A2-full 价格行加时段维度，`resolve()` 按当前时刻选行（粗估：2–3 天，风险：动计费）
- [ ] **A3 SQLite 一致性备份** — 现在 `db-backup/` 是手工 `cp` 的 wal/shm，主库和 WAL 分开拷，
      恢复可能对不上。换成 `sqlite3 .backup` / `VACUUM INTO` + cron + 异地。库才 36M。（粗估：半天）
- [ ] **A4 `npm audit fix`** — nodemailer 两个高危（收件域校验绕过，邮件可投到攻击者域）。
      顺带考虑把 audit 加进 CI 的 verify。（粗估：1 小时）

## B · 运维与健壮性

- [ ] **B1 环境变量转发检查** — 扫 server 源码里的 `process.env.X`，断言每个都在两个 compose 里。
      就是 `TRUST_PROXY` 那类 bug 的防线，现在零防线。（粗估：半天）
- [ ] **B2 生产机容量** — 1.7G 内存剩 100M free，单容器上限 1G → **两人同时开工就 OOM**；磁盘 73%。
      调小 `CONTAINER_MEMORY_MB` 或升配，要你拍板。（粗估：1 小时 / 或花钱）
- [ ] **B3 `/metrics` + 告警** — 余额低、429 激增、排队 p95、磁盘满，现在全靠人看。
      `/gate` 和 `/upstream-allowance` 已有 JSON，先接 exporter。（粗估：2–3 天）
- [ ] **B4 每用户磁盘配额** — 一个人 `npm install` 几次能把盘吃满，连带所有人。（粗估：2–3 天）
- [ ] **B5 CSP 响应头** — Caddyfile 有 HSTS/nosniff/X-Frame-Options，缺 CSP。
      Markdown 渲染 + 文件预览的场景值得有。（粗估：半天，要调 shiki 的白名单）
- [ ] **B6 依赖升级** — shiki 1→4、vite 6→8、TS 5→7、react-markdown 9→10、@fastify/cors 10→11。
      顺带上 Dependabot/Renovate。（粗估：1–2 天，风险：大版本跳）
- [ ] **B7 多实例部署（M5）** — 并发闸门换 Redis 信号量 + 跨进程事件总线（`queue.waiting`
      现在拆进程后就没了）。（粗估：1–2 周）
- [ ] **B8 清掉 `notes.md` 里的明文 key** — 已 gitignore，不会泄漏，纯洁癖。（粗估：5 分钟）

## C · 代码质量与性能

- [ ] **C1 前端测试** — 45 个套件全在 server，web 那边 `chat.ts` 1214 行、`api.ts` 1218 行零覆盖。
      引入 vitest + 首批用例。（粗估：2–3 天）
- [ ] **C2 拆 `handleProxy`** — `gateway/index.ts:247-846`，600 行一个函数，鉴权/路由/闸门/翻译/
      流解析/记账全在里面。（粗估：2–3 天，风险：核心路径）
- [ ] **C3 语言包按需加载** — 8 个 locale 静态打进主 chunk，源码 357KB，主 chunk 888KB。
      改成 `import()`，能砍三分之一以上。（粗估：半天）
- [ ] **C4 长会话虚拟滚动**（已知限制）（粗估：2–3 天）
- [ ] **C5 `protocol.ts` 抽成 workspace 包** — 现在 server/web 各一份，靠 typecheck 逐字比对；
      抽包之后比对脚本能删。（粗估：半天–1 天）
- [ ] **C6 配额轮询改事件推送** — `/api/me/quota` 等，和 B7 一起做更划算。（粗估：1 天）

## D · 用户交互

- [ ] **D1 权限审批 UI + plan 模式** ⭐ — 现在 `PERMISSION_MODE` 默认 `bypassPermissions` 一刀切。
      用 `--permission-prompt-tool stdio` + `--input-format stream-json`，权限请求以
      `control_request` 发出来，前端弹卡片让用户点允许/拒绝，顺带就有 plan 模式。
      **方向 B 的核心。**（粗估：1–2 周，风险：wire 格式官方没写全，要照第三方逆向文档试）
- [ ] **D2 手机推送** — Web Push：turn 完成、需要审批、出错、额度到 90%。（粗估：3–5 天）
- [ ] **D3 对话搜索** — SQLite FTS5，零依赖，M4 已列。（粗估：2–3 天）
- [ ] **D4 分享链接** — 只读、可撤销、可选带不带工具卡片。（粗估：2–3 天）
- [ ] **D5 `/` 命令面板 + 快捷键** — `/compact`、`/clear`、`/model`，`⌘K` 切会话。（粗估：2–3 天）
- [ ] **D6 会话文件夹 / 置顶 / 归档** — 表里有 `parent_id`、`title_custom`，没有 archived/pinned。（粗估：2–3 天）
- [ ] **D7 语音输入** — 浏览器 `SpeechRecognition`，零后端。（粗估：1 天）

## E · 功能

- [ ] **E1 持久项目（目录/git 仓库）跨对话 + git 面板** ⭐ — 现在工作区是
      `workspaces/<user>/<conv>/`，**删对话即删目录**，没法在同一个仓库上开第二个对话。
      这是和 CloudCLI、官方 web 最大的一条差距。**方向 B 的核心。**（粗估：2–3 周，风险：动工作区模型）
- [ ] **E2 MCP 服务器配置**（全局 + 每用户）— 容器里 `--mcp-config` 一挂。（粗估：3–5 天）
- [ ] **E3 定时任务 / Routines** — cron 触发一个对话：日报、依赖升级、代码审查。（粗估：3–5 天）
- [ ] **E4 用户分组 + 组级配额/倍率** — 现在配额只到个人。（粗估：3–5 天）
- [ ] **E5 多凭据池调度** ⭐ — credential-manager 已能存多把凭据，缺「一个模型 → 多凭据 →
      轮转 / 429 换号 / 粘性会话」。sub2api 的核心卖点，拼车场景刚需。**方向 A 的核心。**（粗估：1–2 周）
- [ ] **E6 OIDC 登录** — 组织内部署必备。（粗估：3–5 天）
- [ ] **E7 webhook 通知** — 注册、超额、上游故障。（粗估：2–3 天）
- [ ] **E8 图片输入** — `translate.ts` 已支持 image 块，Composer 不让贴图；`deepseek-flash`
      现在支持视觉。（粗估：1–2 天）
- [ ] **E9 API key 增强** — 加过期时间、模型白名单、单 key 预算。（粗估：2–3 天）
- [ ] **E10 支付 / 兑换码 / 邀请返利** — 方向 A 才需要，对内部署不需要。（粗估：1–2 周）

## F · DeepSeek 专项

> flash 是 $0.15/$0.60（非高峰），Sonnet 5 的 1/13、Opus 5 的 1/33，1M 上下文，支持视觉。
> 便宜到可以改变功能设计，不只是省钱。

- [ ] **F1 「工具模型」独立设置** ⭐ — 标题/摘要/画像/记忆候选这些内部调用
      （`recap.ts:408` 现在跟着 `MODEL` 环境变量走）固定用 flash，和用户选的主模型解耦。
      一次摘要约等于被总结对话 1% 的 token → 「打开画像页才补摘要」可以改成每轮自动，
      进而做语义标签、更好的搜索、自动归类。**最高性价比的一条。**（粗估：1 天）
- [ ] **F2 Codex 直连原生 Responses** — 现在被 `translate.ts` 翻成 chat completions，翻译层
      不带 thinking 块和 cache 标记；且 DeepSeek 明确 chat 格式下 `tools` + thinking 必须回传
      `reasoning_content` 否则 400，翻译层迟早踩。加 `openai-responses` provider kind 透传。（粗估：3–5 天）
- [ ] **F3 自动路由 / 降级** — 简单轮次走 flash、复杂走 pro 或 Claude；额度快用完时自动降级
      而不是直接 402。（粗估：3–5 天）
- [ ] **F4 模型倍率 / 配额单位** — 按 token 计费时 flash 和 Opus 一个权重不合理。
      用已有的按金额模式，或加 new-api 那种模型倍率。（粗估：2–3 天）
- [ ] **F5 按上游分别设并发上限** — 闸门默认 3 是按 Anthropic 订阅调的，DeepSeek 上游
      并发上限 2500，可以单独放宽。**需先确认现在的上限是全局还是按上游**。（粗估：半天）
- [ ] **F6 长上下文的配额提示** — 1M 上下文 + 384K 输出 + `max` 思考，一次能烧很多。（粗估：1 天）

---

## 备注

- 打 ⭐ 的五条是我认为决定产品形态的：D1、E1、E5、F1，以及方向选择本身。
- A 段四条不用选，是欠债 —— 除非你有理由不修，否则我默认它们该做。
- A2 的两个子选项二选一；E8 和 F 段的视觉能力是同一件事，只需勾 E8。
- B7 和 C6 建议一起做；E10 只在方向 A 下有意义。
