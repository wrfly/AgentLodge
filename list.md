# 待选清单

> 从 [RESEARCH.md](./RESEARCH.md) 整理出来的候选项，一条一个勾选框。
> 在 `[ ]` 里打个 `x`，或者直接在行首画 `x` 都行 —— 你选完我按编号做。
>
> 工期是**粗估**，按一个人、含测试和文档；标「风险」的是改核心路径或会动数据。

---

## 方向：已定 —— 要 A 的调度能力，不要它的 SaaS 化

- [x] **方向 A — 团队拼车网关（只取调度部分）**：多凭据池调度（E5）、webhook（E7）、
      自动路由降级（F3）、按上游并发（F5），**这四条按一个整体设计**。
      **明确不做**：支付/兑换码（E10）、OIDC（E6）、用户分组倍率（E4）。
      定位是自用或小团队拼车，不对外售卖。
- [ ] **方向 B — 团队版 Claude Code on the web**：不走。它仅有的两个核心 —— D1（审批 UI）
      和 E1（持久项目 + git 面板）—— 都没选。E3（定时任务）、E8（图片）仍然做，
      但按独立功能做，不往这个方向靠。

---

## A · 紧急修复（线上正在出问题）

- [x] **A1 修 DeepSeek 价格表** `✅ e24961f` — 9/10 新 ID `deepseek-flash` 表里没有，落到 `*` 兜底的
      $5/$25，按金额计费贵 33 倍；`deepseek-v4-pro` 种子价是 4 月 preview 价，少算一半。
      改 `pricing.ts:256` + `model-facts.ts:51`，**线上库要在后台手动改一遍**。（粗估：半天）
- [x] **A2 价格表支持分时** — DeepSeek 周一至五 UTC 01–04、06–10 高峰价 ×2，现在一行只能填一个数。
      两种做法二选一：
      - [ ] ~~A2-min 只在后台显示时段、不动计费~~ —— 不做
      - [x] **A2-full** 价格行加时段维度 `✅ f4553b7 + 7c4ad65`，`resolve()` 按当前时刻选行（粗估：2–3 天，风险：动计费）
            → 和 F4 并进同一批：都改 `model_pricing`，一次改完
- [x] **A3 SQLite 一致性备份** `✅ e24961f（cron 未装）` — 现在 `db-backup/` 是手工 `cp` 的 wal/shm，主库和 WAL 分开拷，
      恢复可能对不上。换成 `sqlite3 .backup` / `VACUUM INTO` + cron + 异地。库才 36M。（粗估：半天）
- [x] **A4 `npm audit fix`** `✅ e24961f` — nodemailer 两个高危（收件域校验绕过，邮件可投到攻击者域）。
      顺带考虑把 audit 加进 CI 的 verify。（粗估：1 小时）

## B · 运维与健壮性

- [x] **B1 环境变量转发检查** `✅ e24961f` — 扫 server 源码里的 `process.env.X`，断言每个都在两个 compose 里。
      就是 `TRUST_PROXY` 那类 bug 的防线，现在零防线。（粗估：半天）
- [x] **B2 生产机容量** `✅ e24961f` — 1.7G 内存剩 100M free，单容器上限 1G → **两人同时开工就 OOM**；磁盘 73%。
      调小 `CONTAINER_MEMORY_MB` 或升配，要你拍板。（粗估：1 小时 / 或花钱）
- [x] **B3 `/metrics` + 告警** — 余额低、429 激增、排队 p95、磁盘满，现在全靠人看。
      `/gate` 和 `/upstream-allowance` 已有 JSON，先接 exporter。（粗估：2–3 天）
- [x] **B4 每用户磁盘配额** — 一个人 `npm install` 几次能把盘吃满，连带所有人。（粗估：2–3 天）
- [x] **B5 CSP 响应头** — Caddyfile 有 HSTS/nosniff/X-Frame-Options，缺 CSP。
      Markdown 渲染 + 文件预览的场景值得有。（粗估：半天，要调 shiki 的白名单）
- [x] **B6 依赖升级** — shiki 1→4、vite 6→8、TS 5→7、react-markdown 9→10、@fastify/cors 10→11。
      顺带上 Dependabot/Renovate。（粗估：1–2 天，风险：大版本跳）
- [x] **B7a 跨进程事件总线** — `queue.waiting` 在 app/gateway 拆进程后就丢了（DESIGN §2.6），
      这是**现在就存在**的缺口；C6 依赖的也正是这半边。（粗估：3–5 天）
- [ ] **B7b Redis 信号量（多实例闸门）** — **推迟**。现在是单机，内存只剩 100M，拉 Redis 进来
      既吃内存，又让「两个文件一条命令」的部署故事变复杂。DESIGN §1 说的是「多实例时要换」，
      而现在还不是多实例。等真要横向扩的时候再做。
- [ ] **B8 清掉 `notes.md` 里的明文 key** — 已 gitignore，不会泄漏，纯洁癖。（粗估：5 分钟）
- [ ] **B9 后台输入框禁止浏览器自动填充** ⭐ `✅ ca29da5` *(2026-09-14 新增，不在最初 41 条里)*
      整个后台**零个** `autoComplete`：共享的 `Input`（`components/ui.tsx`）原样透传 props、没有默认值，
      而全仓唯一用到 `autoComplete` 的是 `PublicPages.tsx` 的注册/重置表单 —— 那里是对的。
      暴露面:`admin/Settings.tsx:315` 把每个 `secret` 设置渲染成裸的 `<input type="password">`
      （`mail.apiKey`、`mail.smtpPassword`）；`admin/Credentials.tsx:297` 是**上游 API key** 框；
      其余纯文本设置（base URL、模型名、配额数字）都能被表单自动填充。
      最坏情况不是难看而是**存错东西**：浏览器把运维自己的登录密码塞进 SMTP 密码或上游 key 框，
      点保存就加密进设置表 / 交给 credential-manager，全程不报错；反向则是密码管理器把上游 key
      当站点密码存下来。
      做法:给 `Input` 一个默认值（放在 `{...props}` **之前**，好让调用方能覆盖），密码类要用
      `autoComplete="new-password"` —— Chrome 对 `off` 在密码框上基本无视。顺带把 `spellCheck={false}`
      从 `Credentials.tsx` 里零散的几处提到组件默认。可选:`data-1p-ignore` / `data-lpignore`
      挡掉 1Password、LastPass。（粗估：1 小时）

## C · 代码质量与性能

- [x] **C1 前端测试** — 45 个套件全在 server，web 那边 `chat.ts` 1214 行、`api.ts` 1218 行零覆盖。
      引入 vitest + 首批用例。（粗估：2–3 天）
- [x] **C2 拆 `handleProxy`** — `gateway/index.ts:247-846`，600 行一个函数，鉴权/路由/闸门/翻译/
      流解析/记账全在里面。（粗估：2–3 天，风险：核心路径）
- [x] **C3 语言包按需加载** — 8 个 locale 静态打进主 chunk，源码 357KB，主 chunk 888KB。
      改成 `import()`，能砍三分之一以上。（粗估：半天）
- [x] **C4 长会话虚拟滚动**（已知限制）（粗估：2–3 天）
- [x] **C5 `protocol.ts` 抽成 workspace 包** — 现在 server/web 各一份，靠 typecheck 逐字比对；
      抽包之后比对脚本能删。（粗估：半天–1 天）
- [x] **C6 配额轮询改事件推送** — `/api/me/quota` 等，和 B7 一起做更划算。（粗估：1 天）

## D · 用户交互

- [ ] **D1 权限审批 UI + plan 模式** ⭐ — 现在 `PERMISSION_MODE` 默认 `bypassPermissions` 一刀切。
      用 `--permission-prompt-tool stdio` + `--input-format stream-json`，权限请求以
      `control_request` 发出来，前端弹卡片让用户点允许/拒绝，顺带就有 plan 模式。
      **方向 B 的核心。**（粗估：1–2 周，风险：wire 格式官方没写全，要照第三方逆向文档试）
- [ ] **D2 手机推送** — Web Push：turn 完成、需要审批、出错、额度到 90%。（粗估：3–5 天）
- [x] **D3 对话搜索** — SQLite FTS5，零依赖，M4 已列。（粗估：2–3 天）
- [x] **D4 分享链接** — 只读、可撤销、可选带不带工具卡片。（粗估：2–3 天）
- [x] **D5 `/` 命令面板 + 快捷键** — `/compact`、`/clear`、`/model`，`⌘K` 切会话。（粗估：2–3 天）
- [x] **D6 会话文件夹 / 置顶 / 归档** — 表里有 `parent_id`、`title_custom`，没有 archived/pinned。（粗估：2–3 天）
- [x] **D7 语音输入** — 浏览器 `SpeechRecognition`，零后端。（粗估：1 天）

## E · 功能

- [ ] **E1 持久项目（目录/git 仓库）跨对话 + git 面板** ⭐ — 现在工作区是
      `workspaces/<user>/<conv>/`，**删对话即删目录**，没法在同一个仓库上开第二个对话。
      这是和 CloudCLI、官方 web 最大的一条差距。**方向 B 的核心。**（粗估：2–3 周，风险：动工作区模型）
- [ ] **E2 MCP 服务器配置**（全局 + 每用户）— 容器里 `--mcp-config` 一挂。（粗估：3–5 天）
- [x] **E3 定时任务 / Routines** — cron 触发一个对话：日报、依赖升级、代码审查。（粗估：3–5 天）
- [ ] **E4 用户分组 + 组级配额/倍率** — 现在配额只到个人。（粗估：3–5 天）
- [x] **E5 多凭据池调度** ⭐ — credential-manager 已能存多把凭据，缺「一个模型 → 多凭据 →
      轮转 / 429 换号 / 粘性会话」。sub2api 的核心卖点，拼车场景刚需。**方向 A 的核心。**（粗估：1–2 周）
- [ ] **E6 OIDC 登录** — 组织内部署必备。（粗估：3–5 天）
- [x] **E7 webhook 通知** — 注册、超额、上游故障。（粗估：2–3 天）
- [x] **E8 图片输入** — `translate.ts` 已支持 image 块，Composer 不让贴图；`deepseek-flash`
      现在支持视觉。（粗估：1–2 天）
- [x] **E9 API key 增强** — 加过期时间、模型白名单、单 key 预算。（粗估：2–3 天）
- [ ] **E11 自带 CLI 的会话统计:按 session、按模型,并给它起个标题** *(2026-09-15 新增)*
      现在的「按会话拆分」只覆盖网页对话 —— 那边每个 conversation 有 id、有标题、有轮次。
      **自带 CLI 打进来的流量没有这些**:`usage_records` 里 `conversation_id` 和 `turn_id` 来自
      运行时票据的 `cid` / `tid`(`gateway/index.ts:857-858`),而 api-key 那条路径两者都是空，
      只有 `api_key_id`。于是这些流量在用量页上只能汇成一坨，看不出「哪一次工作」花了多少。
      想要的是:
        1. 把 CLI 过来的调用**归并成 session**，
        2. 每个 session 给出**按模型拆分**的 token / 花费，
        3. 像网页对话那样**自动起一个标题**(recap.ts 已有这套:一次便宜调用读开头几条消息命名，
           而且 F1 之后这次调用可以走 flash，几乎免费)。
      **未解的关键问题是 session 怎么识别。** 网页那侧是我们自己发的票据；CLI 那侧要么 Claude Code
      本身会发某个会话标识(要抓包确认，`trace-proxy/` 正是干这个的)，要么只能按
      (api_key, 时间间隔) 做启发式归并 —— 后者不可靠，一个人开两个终端就会混在一起。
      所以这条**很可能要配合 proxy 改动**:先用审计代理抓一轮真实的 CLI 流量，看有没有可用的
      会话标识，再决定是读它还是自己造一个。
      与 #47(按上游拆分用量)同一片代码，等它落地后在同一个面上加维度更划算。（粗估：先抓包 1 天，实现 3–5 天）
- [ ] **E10 支付 / 兑换码 / 邀请返利** — 方向 A 才需要，对内部署不需要。（粗估：1–2 周）

## F · DeepSeek 专项

> flash 是 $0.15/$0.60（非高峰），Sonnet 5 的 1/13、Opus 5 的 1/33，1M 上下文，支持视觉。
> 便宜到可以改变功能设计，不只是省钱。

- [x] **F1 「工具模型」独立设置** ⭐ `✅ e24961f` — 标题/摘要/画像/记忆候选这些内部调用
      （`recap.ts:408` 现在跟着 `MODEL` 环境变量走）固定用 flash，和用户选的主模型解耦。
      一次摘要约等于被总结对话 1% 的 token → 「打开画像页才补摘要」可以改成每轮自动，
      进而做语义标签、更好的搜索、自动归类。**最高性价比的一条。**（粗估：1 天）
- [ ] **F2 Codex 直连原生 Responses** — 现在被 `translate.ts` 翻成 chat completions，翻译层
      不带 thinking 块和 cache 标记；且 DeepSeek 明确 chat 格式下 `tools` + thinking 必须回传
      `reasoning_content` 否则 400，翻译层迟早踩。加 `openai-responses` provider kind 透传。（粗估：3–5 天）
- [x] **F3 自动路由 / 降级** — 简单轮次走 flash、复杂走 pro 或 Claude；额度快用完时自动降级
      而不是直接 402。（粗估：3–5 天）
- [x] **F4 模型倍率 / 配额单位** `↩︎ 撤销 —— 已实现` — ~~按 token 计费时 flash 和 Opus 一个权重不合理。~~
      **这条的前提是错的，错在我。** `usage.ts:40-54` 的注释把这个问题原样写着并已解决：
      `billable_tokens = costMicroExact(model) / (兜底行的单个输入 token 价)` —— 一个 billable token
      就是「标准价下的一个输入 token」，每个模型按自己价格的比例换算。flash 的一个 token 本来
      就只抽走 Opus 的 1/33 配额，不需要任何倍率配置。
      我做调研时只读了 `model-facts.ts` 和种子，没读 `usage.ts:55-76`。
      真正让它算错的是 A1 —— `deepseek-flash` 没有价格行时落到兜底价，配额**多扣 33 倍**，A1 修完就对了。
- [x] **F5 按上游分别设并发上限** — 已查证 `gateway/gate.ts`：池子**确实**按上游分
      （`GatePool.for(providerId)`，各自独立的 in-flight 和 AIMD 状态），但
      `setMaxConcurrency` 的注释是「applies to every pool」，`PATCH /gate` 也只收一个数
      —— **上限是全局共用的一个起点**。要给 provider 加上限字段、GatePool 按池取上限、
      后台 UI 改成每条上游一行。（粗估：**1 天**，从半天上调）
- [x] **F6 长上下文的配额提示** `✅ 47f8cec` — 1M 上下文 + 384K 输出 + `max` 思考，一次能烧很多。（粗估：1 天）

---

## G · 计费子系统调研查出的（2026-09-15 新增，不在最初 41 条里）

动 A2-full 之前先派了 8 个并行读者把计费链路整个映射了一遍（价格解析 / 成本计算 / 配额比较 /
价格表 UI / 用户侧额度 / 设置面 / 时区与窗口 / 现有测试）。下面是它查出、而原清单里没有的东西。

### 已经顺手修掉

- [x] **G0 全新安装的价格表是残缺的** `✅ 0fe2eda` —— **这是我在第 1 波引入的回归**。
      迁移 13 写的是数据不是表结构，而 `initDb()` 在全新库上也会跑它：三行 DeepSeek 插进空表，
      `seedDefaults()` 见表非空就返回，于是新部署只有 DeepSeek 三行、没有任何 Claude 价格、
      **没有 `*` 兜底行**。后果全程静默：Claude 模型 `costMicro` 恒为 0，`billable()` 因为
      `resolve('*')` 为空而退回扁平权重。`test:pricing`、`test:schema`、`test:upgrade` 三个套件
      当时全绿 —— 它们从没问过「全新安装长什么样」。新增 `seed.test.ts` 补这一问，在坏代码上 8/12 失败。
- [x] **G1 小数逗号把价格静默设成 0** `✅ 7c4ad65 + 7918898（null 也收紧了）` —— 输入框是纯文本，`Number('0,15')` 是 NaN，
      `JSON.stringify` 写成 null，服务端 `Number(null)` 读成 0。行插进去了、返回 200、模型免费。
      **九种已发布语言里有五种就是用逗号写小数的。**
- [x] **G2 负价格全程放行** `✅ 7c4ad65` —— 会倒给配额。
- [x] **G3 `*` 兜底行一次无确认点击就能删** `✅ 7c4ad65` —— 删掉之后所有未定价模型免费、
      配额退回扁平权重，不报任何错。现在拒绝删最后一条，被取代的旧兜底行仍可清理。
- [x] **G4 看不出哪一行价格在生效** `✅ 7c4ad65` —— 改价是追加行，改两次就有三行长得一样，
      而最可能被「顺手清理掉」的正是在用的那行。
- [x] **G5 删除价格不写审计** `✅ 7c4ad65` —— 新增是写的，删除不写，而删除是更危险的那个。
- [x] **G5b 价格表混用两种货币** `✅ 7918898` —— **又是我在第 1 波引入的**。迁移 13 取被修正行的货币、
      没有行时退回 `'USD'`，而 `deepseek-flash` 从来就没有行。这台机器按 CNY 计费，于是后台上
      ¥2.00 和 $0.1500 并排出现，而所有消费方直接把 `cost_micro` 相加。迁移 15 把那几行撤回
      （不换算 —— 汇率是有日期的决定，这个进程两样都没有），并在日志里列出官方 USD 价让运维自己换算；
      后台现在也会在表里出现两种货币时挂一条警告横幅。这是**部署之后用浏览器看出来的**，单元测试全绿。

### 还没修（按严重度）

- [ ] **G6 切换计费单位不换算上限** ⭐ —— `PATCH /api/admin/users/:id` 写 `limitKind: 'cost'` 时
      不动 `window_limit` 等三列（`routes/admin/users.ts:113-118`）。一个「每月 5,000,000 billable token」
      的上限**静默变成每月 $5.00**，用户开始莫名被拒。反向同理，`boost_amount` 也一样。
      而且整个单位判断挂在一个三元表达式上（`quota.ts:104`，`pool-share.ts:117` 还有一份副本），
      两个分支都是 `number`，写反了能通过类型检查 —— 现有测试也抓不到，因为 `quota.test.ts` 两列写同一个值、
      全部用默认的 `tokens`。（粗估：1 天）
- [ ] **G7 `/api/conversations/:id/quota` 忽略 `:id`** —— 不校验归属、不调 `convRepo.exists`，
      对任何 id（包括不存在的、别人的）都回 200。返回的是调用者自己的配额所以不漏数据，
      但这是个形状在撒谎的路由，而且**全仓没有任何调用方、没有测试**。删掉或补 guard。（粗估：半天）
- [ ] **G8 `import-legacy` 的 `day` 用 UTC，线上用本地** —— `import-legacy.ts:195` 是
      `createdAt.slice(0,10)`，其余全是 `localDay()`。TZ≠UTC 的部署上，导入的历史数据和实时数据
      在日线图上**错开一天**，而上面的总计（按 `created_at` 过滤）是对的。（粗估：半天）
- [ ] **G9 5 小时窗口的回退网格在 DST 区错位** —— `period.ts:133-134` 的
      `new Date(1970, 0, 1, hour)` 取的是 1970 年的偏移（标准时）。读者实测：`TZ=America/New_York`
      下三到十一月的回退窗口从 01:00/06:00/11:00 开始，而不是配置的 00:00。
      只影响「上游还没报过 reset 头」之前的那段。（粗估：半天）
- [ ] **G10 用量页的「本周」和闸门的周不是同一个周** —— 页面用本地日历锚点（`me.ts:106-109`），
      闸门用上游相位锁定的周。上游报过一次之后两者就不一致，
      也就是说**页面上那个「本周用量」不是拦你的那个数**。（粗估：半天）
- [ ] **G11 币种混用无校验** —— `currency` 是每行一个，路由原样透传 `body.currency`，
      而所有消费方直接把 `cost_micro` 相加。一行 CNY 混进 USD 表里，所有金额都失去意义，不报错。
      另外 `fmtMoney` 只认 CNY 和「其它」，设成 EUR 会标着 `(EUR)` 却打印 `$`。（粗估：半天）
- [ ] **G12 provider 级的 `*` 行会压过全局的精确匹配** —— `resolve()` 的 provider 轮次整轮优先，
      不是按具体度优先。给某个上游加一条兜底行，会静默地把那条上游上**所有**模型重新定价。（粗估：半天）
- [ ] **G13 `effective_from` 相同的两行没有 tiebreak** —— SQL 只按 `effective_from` 排序，
      没有 `, id desc`。同一毫秒写入的两行解析顺序随意。（粗估：1 小时）
- [ ] **G14 402 拒绝文案没翻译** —— `quota.ts:174-182` 在服务端拼英文并原样返回，
      而同一批路由的其它错误都走 `tr(req, ...)`。界面是中文、拒绝理由是英文。（粗估：半天）
- [ ] **G15 Composer 靠 JSON key 顺序挑要显示哪个窗口** —— `Object.values(...).find(w => w.exceeded)`。
      现在恰好对，因为 `SCOPES` 的顺序是 window/week/month；重排 `SCOPES` 或任何会重排 key 的
      序列化步骤都会让横幅说错窗口，不报错。（粗估：1 小时）
- [ ] **G16 无上限用户的侧栏固定显示月度** —— `QuotaBar.tsx:35` 在 `tightest` 为 null 时
      写死读 `windows.month.used`，所以一个什么都不限的部署，侧栏显示的是月度消耗、
      标着「无限制」，其它窗口从侧栏根本到不了。（粗估：1 小时）
- [ ] **G17 时段表没有编辑器** —— A2-full 只做了展示：时段由种子和迁移写入，后台能看不能改。
      自己新加一条 DeepSeek 类上游的人配不了高峰。**本次有意不做**（时段是厂商定的、几乎不变，
      为没出现过的场景先做 UI 是投机），记在这里免得以为漏了。（粗估：1 天）
- [ ] **G18 每次 `resolve()` 全表扫描** —— SQL 没有 model 条件，`idx_pricing_model` 用不上；
      `record()` 一行用量要扫四次，而网关**每次上游调用**写一行。现在表只有十几行所以无所谓，
      是个规模问题不是正确性问题。（粗估：半天）

## 执行顺序

选中 32 条，粗估 **65 个工作日**。顺序不是按优先级排的，是按**返工成本**排的 ——
同一张表一次改完，拆完再改，升完再优化。

| 波 | 条目 | 为什么在这个位置 | 粗估 |
|---|---|---|---|
| **1 · 止血** | A1 A4 A3 B1 B2 F1 | 全是当天见效的。F1 放这里不是因为紧急，是因为它每天都在省钱 —— 内部调用换成 flash 之后成本近乎为零 | 6 天 |
| **2 · 计费表** | A2-full F4 F6 | 三条都动 `model_pricing` 和配额展示，分开做等于改三遍 | 5 天 |
| **3 · 地基** | B6 C2 C5 | 先升依赖、先把 600 行的 `handleProxy` 拆开，第 4 波才不用改两遍 | 5 天 |
| **4 · 网关调度**（方向 A 主体） | F5 E5 F3 E7 | 必须在 C2 之后。四条共用调度层，一起设计 | 15 天 |
| **5 · 事件与可观测** | B7a C6 B3 B5 | B7a 必须在 C6 之前 | 8 天 |
| **6 · 前端质量与交互** | C1 → D3+D6 D5 D4 D7 | C1 排在最前：`chat.ts` 1214 行零覆盖，而 D 段全都要动它。D3 和 D6 是同一片侧栏，一起做 | 12 天 |
| **7 · 前端性能** | C3 C4 E8 | B6 升完依赖再优化，否则白做一遍 | 5 天 |
| **8 · 收尾** | B4 E3 E9 | 互相不依赖，谁都可以往前提 | 9 天 |

## 备注

- **没选的 9 条**：B8、D1、D2、E1、E2、E4、E6、E10、F2。其中 F2（Codex 直连 Responses）
  是唯一一条我会再提一次的 —— 不是现在要做，是 DeepSeek 文档明确 chat 格式下
  `tools` + thinking 必须回传 `reasoning_content` 否则 400，翻译层迟早会踩到。
  踩到那天再做也来得及，记在这里免得到时候当灵异事件查。
- A1 要**手动改线上库**（后台 → 系统设置 → 价格表），种子只在首次启动写入。
- 第 4 波是唯一的大件（15 天）。它做完，方向 A 就成立了。
