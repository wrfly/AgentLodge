# 待选清单

> 从 [RESEARCH.md](./RESEARCH.md) 整理出来的候选项，一条一个勾选框。
> 在 `[ ]` 里打个 `x`，或者直接在行首画 `x` 都行 —— 你选完我按编号做。
>
> 工期是**粗估**，按一个人、含测试和文档；标「风险」的是改核心路径或会动数据。

---

## 方向：已定 —— 要 A 的调度能力，不要它的 SaaS 化

- [x] **方向 A — 团队拼车网关（只取调度部分）**：多凭据池调度（E5）、webhook（E7）、
      自动路由降级（F3）、按上游并发（F5），**这四条按一个整体设计**。
      **明确不做**：支付/兑换码（E10）、OIDC（E6）、用户分组倍率（E4）。
      定位是自用或小团队拼车，不对外售卖。
- [ ] **方向 B — 团队版 Claude Code on the web**：不走。它仅有的两个核心 —— D1（审批 UI）
      和 E1（持久项目 + git 面板）—— 都没选。E3（定时任务）、E8（图片）仍然做，
      但按独立功能做，不往这个方向靠。

---

## A · 紧急修复（线上正在出问题）

- [x] **A1 修 DeepSeek 价格表** — 9/10 新 ID `deepseek-flash` 表里没有，落到 `*` 兜底的
      $5/$25，按金额计费贵 33 倍；`deepseek-v4-pro` 种子价是 4 月 preview 价，少算一半。
      改 `pricing.ts:256` + `model-facts.ts:51`，**线上库要在后台手动改一遍**。（粗估：半天）
- [x] **A2 价格表支持分时** — DeepSeek 周一至五 UTC 01–04、06–10 高峰价 ×2，现在一行只能填一个数。
      两种做法二选一：
      - [ ] ~~A2-min 只在后台显示时段、不动计费~~ —— 不做
      - [x] **A2-full** 价格行加时段维度，`resolve()` 按当前时刻选行（粗估：2–3 天，风险：动计费）
            → 和 F4 并进同一批：都改 `model_pricing`，一次改完
- [x] **A3 SQLite 一致性备份** — 现在 `db-backup/` 是手工 `cp` 的 wal/shm，主库和 WAL 分开拷，
      恢复可能对不上。换成 `sqlite3 .backup` / `VACUUM INTO` + cron + 异地。库才 36M。（粗估：半天）
- [x] **A4 `npm audit fix`** — nodemailer 两个高危（收件域校验绕过，邮件可投到攻击者域）。
      顺带考虑把 audit 加进 CI 的 verify。（粗估：1 小时）

## B · 运维与健壮性

- [x] **B1 环境变量转发检查** — 扫 server 源码里的 `process.env.X`，断言每个都在两个 compose 里。
      就是 `TRUST_PROXY` 那类 bug 的防线，现在零防线。（粗估：半天）
- [x] **B2 生产机容量** — 1.7G 内存剩 100M free，单容器上限 1G → **两人同时开工就 OOM**；磁盘 73%。
      调小 `CONTAINER_MEMORY_MB` 或升配，要你拍板。（粗估：1 小时 / 或花钱）
- [x] **B3 `/metrics` + 告警** — 余额低、429 激增、排队 p95、磁盘满，现在全靠人看。
      `/gate` 和 `/upstream-allowance` 已有 JSON，先接 exporter。（粗估：2–3 天）
- [x] **B4 每用户磁盘配额** — 一个人 `npm install` 几次能把盘吃满，连带所有人。（粗估：2–3 天）
- [x] **B5 CSP 响应头** — Caddyfile 有 HSTS/nosniff/X-Frame-Options，缺 CSP。
      Markdown 渲染 + 文件预览的场景值得有。（粗估：半天，要调 shiki 的白名单）
- [x] **B6 依赖升级** — shiki 1→4、vite 6→8、TS 5→7、react-markdown 9→10、@fastify/cors 10→11。
      顺带上 Dependabot/Renovate。（粗估：1–2 天，风险：大版本跳）
- [x] **B7a 跨进程事件总线** — `queue.waiting` 在 app/gateway 拆进程后就丢了（DESIGN §2.6），
      这是**现在就存在**的缺口；C6 依赖的也正是这半边。（粗估：3–5 天）
- [ ] **B7b Redis 信号量（多实例闸门）** — **推迟**。现在是单机，内存只剩 100M，拉 Redis 进来
      既吃内存，又让「两个文件一条命令」的部署故事变复杂。DESIGN §1 说的是「多实例时要换」，
      而现在还不是多实例。等真要横向扩的时候再做。
- [ ] **B8 清掉 `notes.md` 里的明文 key** — 已 gitignore，不会泄漏，纯洁癖。（粗估：5 分钟）

## C · 代码质量与性能

- [x] **C1 前端测试** — 45 个套件全在 server，web 那边 `chat.ts` 1214 行、`api.ts` 1218 行零覆盖。
      引入 vitest + 首批用例。（粗估：2–3 天）
- [x] **C2 拆 `handleProxy`** — `gateway/index.ts:247-846`，600 行一个函数，鉴权/路由/闸门/翻译/
      流解析/记账全在里面。（粗估：2–3 天，风险：核心路径）
- [x] **C3 语言包按需加载** — 8 个 locale 静态打进主 chunk，源码 357KB，主 chunk 888KB。
      改成 `import()`，能砍三分之一以上。（粗估：半天）
- [x] **C4 长会话虚拟滚动**（已知限制）（粗估：2–3 天）
- [x] **C5 `protocol.ts` 抽成 workspace 包** — 现在 server/web 各一份，靠 typecheck 逐字比对；
      抽包之后比对脚本能删。（粗估：半天–1 天）
- [x] **C6 配额轮询改事件推送** — `/api/me/quota` 等，和 B7 一起做更划算。（粗估：1 天）

## D · 用户交互

- [ ] **D1 权限审批 UI + plan 模式** ⭐ — 现在 `PERMISSION_MODE` 默认 `bypassPermissions` 一刀切。
      用 `--permission-prompt-tool stdio` + `--input-format stream-json`，权限请求以
      `control_request` 发出来，前端弹卡片让用户点允许/拒绝，顺带就有 plan 模式。
      **方向 B 的核心。**（粗估：1–2 周，风险：wire 格式官方没写全，要照第三方逆向文档试）
- [ ] **D2 手机推送** — Web Push：turn 完成、需要审批、出错、额度到 90%。（粗估：3–5 天）
- [x] **D3 对话搜索** — SQLite FTS5，零依赖，M4 已列。（粗估：2–3 天）
- [x] **D4 分享链接** — 只读、可撤销、可选带不带工具卡片。（粗估：2–3 天）
- [x] **D5 `/` 命令面板 + 快捷键** — `/compact`、`/clear`、`/model`，`⌘K` 切会话。（粗估：2–3 天）
- [x] **D6 会话文件夹 / 置顶 / 归档** — 表里有 `parent_id`、`title_custom`，没有 archived/pinned。（粗估：2–3 天）
- [x] **D7 语音输入** — 浏览器 `SpeechRecognition`，零后端。（粗估：1 天）

## E · 功能

- [ ] **E1 持久项目（目录/git 仓库）跨对话 + git 面板** ⭐ — 现在工作区是
      `workspaces/<user>/<conv>/`，**删对话即删目录**，没法在同一个仓库上开第二个对话。
      这是和 CloudCLI、官方 web 最大的一条差距。**方向 B 的核心。**（粗估：2–3 周，风险：动工作区模型）
- [ ] **E2 MCP 服务器配置**（全局 + 每用户）— 容器里 `--mcp-config` 一挂。（粗估：3–5 天）
- [x] **E3 定时任务 / Routines** — cron 触发一个对话：日报、依赖升级、代码审查。（粗估：3–5 天）
- [ ] **E4 用户分组 + 组级配额/倍率** — 现在配额只到个人。（粗估：3–5 天）
- [x] **E5 多凭据池调度** ⭐ — credential-manager 已能存多把凭据，缺「一个模型 → 多凭据 →
      轮转 / 429 换号 / 粘性会话」。sub2api 的核心卖点，拼车场景刚需。**方向 A 的核心。**（粗估：1–2 周）
- [ ] **E6 OIDC 登录** — 组织内部署必备。（粗估：3–5 天）
- [x] **E7 webhook 通知** — 注册、超额、上游故障。（粗估：2–3 天）
- [x] **E8 图片输入** — `translate.ts` 已支持 image 块，Composer 不让贴图；`deepseek-flash`
      现在支持视觉。（粗估：1–2 天）
- [x] **E9 API key 增强** — 加过期时间、模型白名单、单 key 预算。（粗估：2–3 天）
- [ ] **E10 支付 / 兑换码 / 邀请返利** — 方向 A 才需要，对内部署不需要。（粗估：1–2 周）

## F · DeepSeek 专项

> flash 是 $0.15/$0.60（非高峰），Sonnet 5 的 1/13、Opus 5 的 1/33，1M 上下文，支持视觉。
> 便宜到可以改变功能设计，不只是省钱。

- [x] **F1 「工具模型」独立设置** ⭐ — 标题/摘要/画像/记忆候选这些内部调用
      （`recap.ts:408` 现在跟着 `MODEL` 环境变量走）固定用 flash，和用户选的主模型解耦。
      一次摘要约等于被总结对话 1% 的 token → 「打开画像页才补摘要」可以改成每轮自动，
      进而做语义标签、更好的搜索、自动归类。**最高性价比的一条。**（粗估：1 天）
- [ ] **F2 Codex 直连原生 Responses** — 现在被 `translate.ts` 翻成 chat completions，翻译层
      不带 thinking 块和 cache 标记；且 DeepSeek 明确 chat 格式下 `tools` + thinking 必须回传
      `reasoning_content` 否则 400，翻译层迟早踩。加 `openai-responses` provider kind 透传。（粗估：3–5 天）
- [x] **F3 自动路由 / 降级** — 简单轮次走 flash、复杂走 pro 或 Claude；额度快用完时自动降级
      而不是直接 402。（粗估：3–5 天）
- [x] **F4 模型倍率 / 配额单位** — 按 token 计费时 flash 和 Opus 一个权重不合理。
      用已有的按金额模式，或加 new-api 那种模型倍率。（粗估：2–3 天）
- [x] **F5 按上游分别设并发上限** — 已查证 `gateway/gate.ts`：池子**确实**按上游分
      （`GatePool.for(providerId)`，各自独立的 in-flight 和 AIMD 状态），但
      `setMaxConcurrency` 的注释是「applies to every pool」，`PATCH /gate` 也只收一个数
      —— **上限是全局共用的一个起点**。要给 provider 加上限字段、GatePool 按池取上限、
      后台 UI 改成每条上游一行。（粗估：**1 天**，从半天上调）
- [x] **F6 长上下文的配额提示** — 1M 上下文 + 384K 输出 + `max` 思考，一次能烧很多。（粗估：1 天）

---

## 执行顺序

选中 32 条，粗估 **65 个工作日**。顺序不是按优先级排的，是按**返工成本**排的 ——
同一张表一次改完，拆完再改，升完再优化。

| 波 | 条目 | 为什么在这个位置 | 粗估 |
|---|---|---|---|
| **1 · 止血** | A1 A4 A3 B1 B2 F1 | 全是当天见效的。F1 放这里不是因为紧急，是因为它每天都在省钱 —— 内部调用换成 flash 之后成本近乎为零 | 6 天 |
| **2 · 计费表** | A2-full F4 F6 | 三条都动 `model_pricing` 和配额展示，分开做等于改三遍 | 5 天 |
| **3 · 地基** | B6 C2 C5 | 先升依赖、先把 600 行的 `handleProxy` 拆开，第 4 波才不用改两遍 | 5 天 |
| **4 · 网关调度**（方向 A 主体） | F5 E5 F3 E7 | 必须在 C2 之后。四条共用调度层，一起设计 | 15 天 |
| **5 · 事件与可观测** | B7a C6 B3 B5 | B7a 必须在 C6 之前 | 8 天 |
| **6 · 前端质量与交互** | C1 → D3+D6 D5 D4 D7 | C1 排在最前：`chat.ts` 1214 行零覆盖，而 D 段全都要动它。D3 和 D6 是同一片侧栏，一起做 | 12 天 |
| **7 · 前端性能** | C3 C4 E8 | B6 升完依赖再优化，否则白做一遍 | 5 天 |
| **8 · 收尾** | B4 E3 E9 | 互相不依赖，谁都可以往前提 | 9 天 |

## 备注

- **没选的 9 条**：B8、D1、D2、E1、E2、E4、E6、E10、F2。其中 F2（Codex 直连 Responses）
  是唯一一条我会再提一次的 —— 不是现在要做，是 DeepSeek 文档明确 chat 格式下
  `tools` + thinking 必须回传 `reasoning_content` 否则 400，翻译层迟早会踩到。
  踩到那天再做也来得及，记在这里免得到时候当灵异事件查。
- A1 要**手动改线上库**（后台 → 系统设置 → 价格表），种子只在首次启动写入。
- 第 4 波是唯一的大件（15 天）。它做完，方向 A 就成立了。
